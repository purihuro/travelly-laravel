@extends('admin.layouts.app')
@section('title', 'Pesan Kontak')
@section('page_title', 'Pesan Kontak')
@section('page_description', 'Kelola inbox pertanyaan dan permintaan pelanggan.')
@section('content')
<section class="panel" style="margin-bottom:18px;"><div class="panel-body"><form method="GET" action="{{ route('admin.contacts.index') }}" class="form-grid"><div class="field"><label for="search">Cari</label><input id="search" type="text" name="search" value="{{ request('search') }}" placeholder="Nama, email, subjek"></div><div class="field"><label for="status">Status</label><select id="status" name="status"><option value="">Semua status</option>@foreach (['new', 'read', 'replied', 'archived'] as $status)<option value="{{ $status }}" {{ request('status') === $status ? 'selected' : '' }}>{{ ucfirst($status) }}</option>@endforeach</select></div><div class="field"><label for="date_from">Dari Tanggal</label><input id="date_from" type="date" name="date_from" value="{{ request('date_from') }}"></div><div class="field"><label for="date_to">Sampai Tanggal</label><input id="date_to" type="date" name="date_to" value="{{ request('date_to') }}"></div><div class="field" style="justify-content:end;"><label>&nbsp;</label><div class="actions"><button class="btn btn-primary" type="submit">Filter</button><a class="btn btn-secondary" href="{{ route('admin.contacts.index', array_filter(array_merge(request()->query(), ['export' => 'csv']))) }}">Export CSV</a><a class="btn btn-secondary" href="{{ route('admin.contacts.index', array_filter(array_merge(request()->query(), ['export' => 'xlsx']))) }}">Export XLSX</a><a class="btn btn-ghost" href="{{ route('admin.contacts.index') }}">Reset</a></div></div></form></div></section>
<section class="panel"><div class="panel-body"><div class="table-wrap"><table><thead><tr><th>Nama</th><th>Subjek</th><th>Status</th><th>Dikirim</th><th>Aksi</th></tr></thead><tbody>@forelse ($messages as $message)<tr><td><strong>{{ $message->full_name }}</strong><div class="muted">{{ $message->email }}</div></td><td>{{ $message->subject ?: 'Tanpa subjek' }}</td><td><span class="badge {{ $message->status === 'new' ? 'badge-warning' : ($message->status === 'replied' ? 'badge-success' : 'badge-neutral') }}">{{ $message->status_label }}</span></td><td>{{ $message->created_at?->format('d M Y H:i') }}</td><td><div class="actions"><a class="btn btn-secondary" href="{{ route('admin.contacts.show', $message) }}">Lihat</a><a class="btn btn-ghost" href="{{ route('admin.contacts.edit', $message) }}">Edit</a></div></td></tr>@empty<tr><td colspan="5" class="muted">Belum ada pesan kontak.</td></tr>@endforelse</tbody></table></div><div class="pagination">{{ $messages->links() }}</div></div></section>
@endsection
