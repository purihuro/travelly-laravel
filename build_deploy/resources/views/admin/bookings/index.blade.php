@extends('admin.layouts.app')
@section('title', 'Booking')
@section('page_title', 'Booking')
@section('page_description', 'Pantau transaksi pemesanan dan status pembayaran pelanggan.')
@section('content')
<section class="panel" style="margin-bottom:18px;"><div class="panel-body"><form method="GET" action="{{ route('admin.bookings.index') }}" class="form-grid"><div class="field"><label for="search">Cari</label><input id="search" type="text" name="search" value="{{ request('search') }}" placeholder="Kode booking, nama, email"></div><div class="field"><label for="booking_status">Status Booking</label><select id="booking_status" name="booking_status"><option value="">Semua status</option>@foreach (['pending', 'confirmed', 'completed', 'cancelled'] as $status)<option value="{{ $status }}" {{ request('booking_status') === $status ? 'selected' : '' }}>{{ ucfirst($status) }}</option>@endforeach</select></div><div class="field"><label for="payment_status">Status Pembayaran</label><select id="payment_status" name="payment_status"><option value="">Semua pembayaran</option>@foreach (['unpaid', 'paid', 'refunded'] as $status)<option value="{{ $status }}" {{ request('payment_status') === $status ? 'selected' : '' }}>{{ ucfirst($status) }}</option>@endforeach</select></div><div class="field"><label for="date_from">Dari Tanggal</label><input id="date_from" type="date" name="date_from" value="{{ request('date_from') }}"></div><div class="field"><label for="date_to">Sampai Tanggal</label><input id="date_to" type="date" name="date_to" value="{{ request('date_to') }}"></div><div class="field" style="justify-content:end;"><label>&nbsp;</label><div class="actions"><button class="btn btn-primary" type="submit">Filter</button><a class="btn btn-secondary" href="{{ route('admin.bookings.index', array_filter(array_merge(request()->query(), ['export' => 'csv']))) }}">Export CSV</a><a class="btn btn-secondary" href="{{ route('admin.bookings.index', array_filter(array_merge(request()->query(), ['export' => 'xlsx']))) }}">Export XLSX</a><a class="btn btn-ghost" href="{{ route('admin.bookings.index') }}">Reset</a></div></div></form></div></section>
<section class="panel"><div class="panel-body"><div class="table-wrap"><table><thead><tr><th>Kode</th><th>Pelanggan</th><th>Status</th><th>Item</th><th>Pembayaran</th><th>Total</th><th>Aksi</th></tr></thead><tbody>@forelse ($bookings as $booking)<tr><td>{{ $booking->booking_code }}</td><td><strong>{{ $booking->customer_full_name }}</strong><div class="muted">{{ $booking->customer_email }}</div></td><td><span class="badge badge-warning">{{ $booking->status_badge }}</span></td><td><span class="muted">Paket: {{ $booking->items_count }} | Destinasi: {{ $booking->destination_items_count }} | Kuliner: {{ $booking->culinary_count }}</span></td><td><span class="badge badge-neutral">{{ $booking->payment_badge }}</span></td><td>${{ number_format((float) $booking->total_amount, 2) }}</td><td><div class="actions"><a class="btn btn-secondary" href="{{ route('admin.bookings.show', $booking) }}">Lihat</a><a class="btn btn-ghost" href="{{ route('admin.bookings.edit', $booking) }}">Edit</a></div></td></tr>@empty<tr><td colspan="7" class="muted">Belum ada booking.</td></tr>@endforelse</tbody></table></div><div class="pagination">{{ $bookings->links() }}</div></div></section>
@endsection
